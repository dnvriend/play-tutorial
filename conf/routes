# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET        /user                    controllers.User.editUser
POST       /user                    controllers.User.postUser
